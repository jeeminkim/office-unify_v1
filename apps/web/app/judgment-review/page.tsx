import { JudgmentReviewClient } from './JudgmentReviewClient';

export const metadata = {
  title: '30일 판단 품질 복기',
  description: '판단 과정·반복 실수·행동 개선 중심의 30일 복기 리포트',
};

export default function JudgmentReviewPage() {
  return <JudgmentReviewClient />;
}
